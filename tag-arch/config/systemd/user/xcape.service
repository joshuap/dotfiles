[Unit]
Description=Xcape Daemon

[Service]
Type=forking
ExecStart=/usr/bin/xcape -e "Hyper_L=Tab;Hyper_R=backslash"
ExecStop=/usr/bin/pkill xcape
Restart=always
RestartSec=1

[Install]
WantedBy=wm.target
