#!/bin/sh

# This file is executed by xinit.

# Note: I'm intentionally ignoring xinitrc.d scripts which I don't control.
# if [ -d /etc/X11/xinit/xinitrc.d ]; then
#   for f in /etc/X11/xinit/xinitrc.d/*; do
#     [ -x "$f" ] && . "$f"
#   done
#   unset f
# fi

[ -f $HOME/.xprofile ] && . $HOME/.xprofile

exec i3
